// Combine variables

const firstName = `Mahim`;
const lastName = `Zayn`;

const fullName = firstName + ` ` + lastName;

const fullName2 = firstName.concat(` `).concat(lastName);
console.log(fullName2);