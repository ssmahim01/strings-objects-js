// declare

const pen = { brand: 'Matador', price: 12, color: 'black' }
console.log(pen);

const pencil = new Object({ price: 15, color: 'yellow'});
console.log(pencil);

const rubber = Object.create();
console.log(rubber);