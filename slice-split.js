// Slice

const division = `Sylhet Sadar`;
const part = division.slice(0, 7);
console.log(part);

// Split

const message = `I am a hardworking person and have perseverance`;
console.log(message.split(` `));

const friends = `mahim, nahim, dalim, sahim, rahim`;
console.log(friends.split(','));

// join()

const friends1 = [ 'mahim', ' nahim', ' dalim', ' sahim', ' rahim' ];
console.log(friends1.join(` | `));